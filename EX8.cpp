#include<iostream>
#include<string>
#include<time.h>
#include <cstdlib>
void main() {
	using namespace std;
	int serteio[5], aposta[5], i, j , acertou;
	string sorteio[5];
		srand(time(0));
		for (i = 0; i < 5; i++)
		{
			sorteio[i] = rand() % 50 + 1;
			cout << sorteio[5] << "\n";




				cin >> aposta[5];

		}
		
		{	
			if(sorteio == aposta);
			
		}
}

